pipeline {
    agent any

    stages {
        stage('Update Dependencies') {
            steps {
                sh "chmod +x scripts/*"

                sh "scripts/clean.sh"

                sh "mkdir tools"
                sh "scripts/install-phive.sh --local"

                sh "tools/phive update"
                sh "tools/composer update"
            }
        }

        stage('Execute Phing Tasks') {
            steps {
                phing "all"
            }
        }
    }
}