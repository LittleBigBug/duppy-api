pipeline {
    agent any

    stages {
        stage('Update Dependencies') {
            steps {
                sh "chmod +x scripts/*"

                sh "scripts/clean.sh"
                sh "composer update"
            }
        }

        stage('Execute Phing Tasks') {
            steps {
                phing "all"
            }
        }
    }
}